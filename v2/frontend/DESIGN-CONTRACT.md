# Frozen V2 UI reference

> This file preserves the approved prototype-era visual decisions and historical
> QA routes as an acceptance reference. The production entry in `src/App.jsx`
> does not implement those query-string demos or synthetic data; it consumes
> the same-origin Flask `/api/v1` contract described in `../docs/API-CONTRACT.md`.

## Prototype source instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Selected visual direction

- Source of truth: the production structure in `src/App.jsx`, the ordered styles under `src/styles/`, and the executable contract tests under `tests/`. Earlier private mockups are historical design inputs, not public repository dependencies.
- The approved “我的预约” direction uses the existing open-canvas page and right-side inspector.
- The approved appointment-tag direction uses an ultra-pale whole-card tint and a small solid color dot. Do not repeat the tag name on calendar cards; the color already carries that signal.
- Offer exactly four low-saturation tag colors in the prototype: clay terracotta, muted ochre, sage, and misty slate blue. Keep tags separate from system status, allow one primary tag per appointment, and do not expand the palette unless the user asks.
- Keep those four colors as fixed visual slots and use only neutral default names (“标签 1–4”). Slots 1–2 are named centrally by administrators; slots 3–4 are named separately by each user. Tag filtering, new-booking choices, and booking details must use the applicable latest names. When an administrator filters by a personal tag, require selecting its user first; do not prescribe business-specific labels such as “资料核对”.
- Booking forms keep the four tag choices in one compact row. Wrap each visible label in its shrinkable text slot so a long custom name truncates with an ellipsis inside its own button; preserve the complete name in the button's accessible name and hover title, and never allow text to bleed into a neighboring choice or the notes row.
- “我的预约” is the default prototype view. Preserve the original left icon rail and a compact sans-serif page title. The top-right action is a small quiet filter icon, never a “新建预约” button.
- The next booking focus contains only the time, a terracotta status dot inline with the room name, and a right chevron. Details open in the existing right-side inspector.
- Use a medium `400` sans-serif weight for the large next-booking time, matching the user's preferred older version rather than an ultra-light display weight.
- The “我的预约” top-right filter must support both room and custom-tag filtering. Keep the filter icon, active indicator, checkboxes, reset action, and focus treatment black/graphite rather than terracotta.
- The calendar toolbar does not need a separate “新建预约” button because empty slots already create reservations. Keep only the date controls and a black/graphite tag filter.
- Keep the initial “之后” list compact at two low-contrast rows. The centered “更多预约” control expands later upcoming tasks in place; it must not open historical bookings.
- Historical bookings live on a dedicated main page rather than inside the “我的预约” expansion.
- History is a dedicated main page, while clicking a record opens that record's details in the existing right-side inspector.
- Keep history month navigation at the top of the content area with previous/next controls and a compact year-month dropdown. Never add a second month sidebar beside the global icon rail.
- History rows use only three visual anchors: a large day plus muted weekday, a time/room line plus low-contrast case number, and a tiny tag-color dot plus chevron. Do not show party names, status labels, or tag names in the default list; reveal them through search, filters, and the detail inspector.
- Preserve the existing history list and place additional permission filters in one roomy popover anchored below the existing top-right filter button; filters apply immediately, so do not add an “应用” button.
- “预约记录” is role-scoped before room, tag, or search filtering. Ordinary employees can only see their own synthetic records and receive a fixed “仅显示本人的预约记录” explanation; they must never receive a control that expands the range. Administrators default to “全单位预约”, may switch to “仅我的预约”, and may optionally select one enabled user. Do not add employee names to every history row.
- Unit tag slots 1–2 stay available in all-unit history filtering. Personal tag slots 3–4 are disabled until an administrator selects the corresponding user; the disabled state must explain the dependency and provide a focusable “选择用户” action. Changing or clearing the selected user resets the personal-tag subset, and “重置筛选” restores the administrator's all-unit/no-user baseline.
- Tag-name editing follows the same ownership boundary: administrators can edit centrally managed slots 1–2 plus their own slots 3–4, while ordinary employees see slots 1–2 as read-only and can edit only their own slots 3–4. Keep the four fixed colors and neutral default names “标签 1–4”; the owner context belongs in the group heading, not in the default history rows.
- History permission demo routes are `?screen=history&role=admin&historyUser=lijing`, `?screen=history&role=admin`, and `?screen=history&role=employee`. Owner ids and filtering are React synthetic-memory demonstrations only; they do not establish production authentication, authorization, API, Flask, SQLite, or customer-data behavior. The final frozen visual contract is defined by the V2 UI freeze snapshot below.
- This is a non-AI operational appointment product. Do not add chat, assistant, model controls, marketing content, or AI metaphors.
- Implement login as one uninterrupted warm-ivory canvas: a calm sans-serif login form on the left and the project-owned neutral schedule/portal illustration on the right. Do not restore a card, center divider, top-left product name, version number, LAN helper copy, or bottom data-retention copy.
- Login labels and controls must feel integrated with the canvas: use the PingFang/HarmonyOS/system sans stack, restrained warm borders, 8px input corners, a 44px password-visibility target, and a black/graphite primary action. Terracotta is limited to the small title dot, validation border/text, and the smaller illustration disc.
- The login prototype must provide operable synthetic states for idle, wrong password, disabled account, signing in, and service failure, plus required-field errors and a successful transition into the existing app. It must not call a real authentication service or read production data.
- The first-run setup uses a guided two-plane composition: a slightly darker warm support plane on the left for the five-step sequence and a raised-ivory open workspace on the right. Do not place the wizard in a centered card or reuse the authenticated icon rail.
- First-run setup is one continuous five-step flow: security explanation, first administrator, at least one recording room, working hours, and final confirmation/completion. The administrator step keeps username/name in one row, department below, then a hairline and vertically stacked password fields on desktop-height canvases. Keep the selected mock's quiet Phosphor completion and active-step marks.
- The setup defaults are 08:30–17:30, 30-minute granularity, and a 180-minute maximum booking duration. Validate all administrator fields, matching passwords, at least one non-empty room, and an end time later than the start time. Before returning to login, commit the configured administrator, rooms, and half-hour working slots to the same synthetic React memory used by the prototype; the newly created administrator must be able to sign in during that SPA session. This still uses no Flask, SQLite, API, or customer data and makes no reload-persistence promise.
- Keep user management as an open editorial roster with four columns (user, department, role, status), quiet hairline rows, a compact black-plus create action, and the existing right-side inspector. Do not turn it into a dense admin table, card grid, dashboard, or separate modal system.
- User management is administrator-only. Administrators can search the synthetic roster, create users, edit name/department/administrator-or-employee role/enabled state, and reset passwords. Ordinary employees must not see the user-management rail item; a direct employee demo route falls back to “我的预约”. Do not add phone, email, avatar, customer identifiers, or external account integrations.
- The system must always keep at least one enabled administrator. If the selected account is the final enabled administrator, disable both demotion and account deactivation and show an explicit blocking explanation with the next action (create or enable another administrator first).
- User drawers reuse the product's 430px inspector and Phosphor icons. At 1280px and wider they sit beside the roster without dimming it. At 1024px the open roster contracts to `用户 + 角色/状态`, keeping the live permission/status preview visible beside the overlaying drawer; department and the row chevron may be hidden only in this open-drawer minimum-width state. Opening a drawer moves focus to the first editable field, Tab/Shift+Tab stay trapped, Esc closes it, and focus returns to the triggering control. All user and password changes remain synthetic React memory state only.
- Password reset is a low-frequency account-security action, not a peer of the primary save action. Keep it as a quiet 44px-hit-area text row inside the drawer content; the fixed footer contains only the black “保存修改” action.
- While an administrator edits name, department, role, or enabled state, the selected roster row previews the draft immediately. Unsaved changes carry a small “待保存” label, and an enabled-state change explains “左侧状态已预览，保存后正式生效”; clicking “保存修改” removes the pending marker and commits the synthetic in-memory value.
- The selected V2 system-status target is the first displayed ideation result, preserved in-project at `qa/v2-system/source-system-status-option-1.png`. Keep it as one open editorial status ledger: a calm overall-health line, two hairline-separated groups for runtime and service connection, and one quiet outlined diagnostic-export action. Do not turn it into metric cards, charts, a dashboard, a monitoring wall, or a settings form.
- System status is administrator-only. Its rail item uses the existing Phosphor `Pulse` icon and the same sparse terracotta active marker; ordinary employees see neither “系统状态” nor “用户管理”, and direct employee demo routes fall back to “我的预约”.
- The runtime ledger includes one quiet “工作时间” row. The whole row is the edit entry, matching “最近备份” with a trailing chevron instead of adding a standalone button inside the value cell. It opens the existing right inspector with two 30-minute-aligned time inputs, field errors, and a disabled busy save action. The footer action remains separated from the last input by flexible space plus a visible top gap. Keep this inside the editorial ledger; do not turn System Status into a settings dashboard or inline form.
- The page may display only de-identified operational facts: application version, database version, LAN address, latest backup time, API status, public-display status, and base health. Never display passwords, keys, appointment contents, parties, case numbers, notes, tag names, or customer data.
- “工作时间” and “最近备份” are the two ledger rows with a chevron. Each opens the existing 430px inspector, moves focus inside, traps Tab/Shift+Tab, closes with Esc, and returns focus to the triggering row. The backup drawer reports only backup execution metadata and repeats the privacy boundary.
- “导出诊断信息” downloads a synthetic JSON prototype artifact and shows operable exporting and success feedback. The file is deliberately de-identified and contains no production diagnostics, Flask/API response, SQLite data, credentials, reservations, or personal information.
- Keep normal, warning, and offline system profiles available as synthetic in-memory demo states through `systemState=normal|warning|offline`. Warning/offline views must identify the affected service and offer a clear “重新检查” action; successful recovery returns to the normal profile and confirms the action.
- The selected public LAN display target is the third displayed masked-name ideation result, preserved in-project at `qa/v2-display/source-public-display-option-3.png`. Keep its calm horizontal service register: date/time and connection health in one open header, then three full-width hairline rows ordered as room, “请前往”, current visitor, and next visitor. Do not add the authenticated icon rail, cards, charts, rankings, advertising, or operational metrics.
- The public display is an unauthenticated LAN-only route at `?screen=display`. It is intended for service visitors rather than staff and may show only the minimum wayfinding information approved by the user: destination room, a masked Chinese name in the form “张*燕”, the current time range, and the next masked visitor/time range. Never show a full name, appointment owner/employee, case number, purpose, notes, labels, phone, department, or other identifying information.
- The public display must always show current date/time, last-update time, room name, current call and next call. Normal, loading, stale-data, and offline synthetic states are available through `displayState=normal|loading|stale|offline`. Stale/offline states keep the last successful masked roster visible, explain what happened, and provide a 44–46px “立即刷新” or “重新连接” action; success returns to normal and announces recovery.
- If the server returns `rooms: []` because every room is excluded from public display, retain the public-display shell, clock, health and footer while showing one calm empty state; do not synthesize or recover hidden room data in the browser.
- Public-display rows must be derived from the same synthetic `rooms + bookings` memory as the calendar, then passed through an explicit public-field selector and name-masking function. Room rename/deactivation and synthetic schedule changes therefore affect the projected public rows. The prototype still does not expose a real public endpoint, poll Flask, read SQLite, or access customer/production records. Formal integration must repeat the allowlist and masking on the server; the client projection is a testable UI contract, not a production privacy boundary.
- Keep the established qualities: warm ivory canvas, narrow icon-only rail, restrained labels, generous whitespace, graphite hairlines, and terracotta as a rare interaction accent.
- Data Center weekday-by-time analysis uses one continuous half-hour dot plot rather than separate morning/afternoon progress bars. Keep seven weekday rows, a truthful continuous work-time axis without an invented lunch break, restrained time labels, dot size plus opacity for volume, one exact peak annotation, hover/focus details, and a small low/medium/high legend. Empty slots remain faint so users can see when work is quiet; do not turn this view into a rectangular rainbow heatmap or a utilization percentage.
- The schedule is an open time canvas, not a boxed table: no outer table card, no vertical grid, no repeated availability dots or labels. Blank time slots mean available.
- The seeded calendar covers the full 08:30–17:30 working range in half-hour slots, including afternoon availability. A completed first-run setup may replace that range in synthetic memory with the configured start/end times, still at 30-minute granularity.
- Available slots reveal a thin rounded outline and create affordance on hover/focus.
- Existing reservations are the only persistent colored blocks.
- The right booking inspector is closed by default and slides in only after a slot is clicked.
- The light theme uses the project's layered warm-neutral system instead of a single bright field: main work canvas `#F5F4ED`, rail/supporting plane `#F0EEE6`, and raised surfaces `#FAF9F5`. Keep persistent schedule hairlines around 7–8% ink opacity so the dense calendar does not feel visually sharp.
- Preserve customer-data boundaries: this prototype uses synthetic in-memory data only and must not read or write the production SQLite database.
- The history view's rail label and page title are both “预约记录”; keep them identical when renaming either.
- Header tool buttons share one 46px hit size across “我的预约”, “预约日历”, and “预约记录”.
- “我的预约” keeps the original centered hero composition: the next booking is a centered max-620px block with an absolute right chevron, and the “之后” column is centered at 880px. A left-aligned editorial variant was tried on 2026-08-01 and explicitly rejected by the user — do not left-align this page again.
- Filter popover checkboxes use a custom graphite checkbox (appearance: none, ink-secondary fill, raised-paper check) instead of the native black accent.
- The booking detail inspector shows the time once in the heading. The date remains the only subtitle text, while the adjacent status badge remains visible so active and cancelled reservations are distinguishable without leaving the inspector. Its footer action is a quiet secondary button, not the black primary.
- History rows are tuned for scan reading: ~124px rows, day numeral clamp(32px, 3vw, 44px), and month steppers grouped [<] [>] before the year-month dropdown, with the count after it.
- History supports progressive loading: a quiet centered “加载{月份}的记录” button at the bottom of the list appends the next older month in place, separated by a muted month divider. Every appended month and its cursor pages reuse the active scope, user, room, status, tag, and search filters; the month steppers and dropdown always reset back to the single selected month.
- History typography uses a calm system-sans hierarchy rather than one uniformly dark display treatment: medium title/month controls, restrained day numerals, smaller weekdays and case numbers, and a quieter room label beside the time. Preserve this separation when changing history density.
- Room management uses three open vertical room columns separated only by graphite hairlines. Do not turn this page into cards, a dense admin table, or a room-photo dashboard.
- Each room column keeps only the sort number, room name, availability state, today/future counts, and next booking. The room-name row itself is the real management button with a quiet caret; do not repeat “管理笔录室” at the bottom of every column. Creation and editing belong in the existing right inspector, not inline in the overview.
- The existing room inspector contains a second quiet switch labeled “在公开大屏显示”, defaulting on for a new room. It stays inside the inspector and never adds a control or status badge to the room column or overview. Turning it off relies on the public display's normal polling; the authenticated room/calendar surfaces do not hide the room.
- On the room page, the first column's content aligns exactly with the page-title left edge. The add-room action is an icon-only black plus button; its visible Chinese label appears only as the hover/focus tooltip “添加笔录室”, while the button keeps the same accessible name.
- When the room overview wraps beyond three rooms, every new row's first room (4, 7, 10, etc.) must also remove its left divider and use the same zero left inset as the first room.
- Disabling a room immediately hides it from the appointment calendar while preserving existing reservations. Deletion remains unavailable while future reservations exist; a newly created empty room can be deleted. The prototype keeps these changes in synthetic in-memory state only.
- The booking-duration control was revised after the option-1 mock. New and edited bookings use one fixed-length Codex-like progress slider spanning 30–180 minutes in 30-minute steps. The two endpoint stops never show dots; only the four interior 60/90/120/150-minute stops are visible, and each reflects reached, still-available, or dynamically unavailable state. Keep the track length stable when supported durations change. Use a saturated terracotta gradient for the selected range, a quieter warm gradient for the still-available range, and a neutral graphite-beige track for unavailable time. Every warm gradient must end exactly at the last available duration and must never bleed into the unavailable track. The visible knob is a restrained warm off-white solid sphere with only a hairline border and soft shadow; do not restore the Liquid Glass highlight, refraction ring, transparency, or blur treatment. The native range thumb remains transparent underneath as the interaction target. Do not restore the segmented control, the “更多” menu, or the helper copy “最短30分钟 · 每次增加30分钟”.
- Upcoming booking details use a black primary “修改预约” action and a restrained outlined “取消预约” action at the bottom of the existing inspector. Cancellation requires a confirmation step. Completed history details remain read-only and use only “关闭”.
- Booking edits reuse the right inspector, preserve the appointment identity, and recheck room/date/start/duration conflicts before saving. Keep the prototype state synthetic and in-memory.
- The selected “预约刚被别人占用” target is the second displayed conflict ideation result, preserved at `qa/v2-feedback/source-booking-conflict-option-2.png`. When a create request loses a race, keep the calendar visible with the newly occupied block and transform the existing right drawer into one focused resolution state; do not stack a modal, discard the form, or replace the calendar with a full-page error.
- Conflict copy must state exactly what changed (`笔录室 + 开始/结束时间`) and explicitly confirm that name, case number, tag, and notes were preserved. The drawer shows a hairline-separated draft summary and three real actions: black “返回日历重新选择”, outlined “保留草稿并关闭”, and quiet “重新检查这个时段”. Recheck reloads the target date, exposes a busy state, and reports exactly whether the slot is still occupied or is now available; the two exit actions preserve the draft.
- Returning to the calendar keeps a persistent, dismissible “待续草稿” notice with the preserved party, case number, original room, date, and time. Selecting another empty slot must first open the existing right drawer and compare the original and target slots; the user explicitly chooses “使用草稿预约此时段”, “清除草稿并新建”, or “返回日历”. Only the first action restores the preserved name, case number, tag, and notes while replacing room/date/start/duration. Never silently apply a preserved draft after the short toast has disappeared. Opening, state replacement, Tab/Shift+Tab trapping, Esc closing, and focus return continue to use the existing drawer accessibility behavior.
- Synthetic demo routes are `?screen=calendar&bookingState=ready` for the pre-submit form and `?screen=calendar&bookingState=conflict` for the resolved conflict state. The simulated competing reservation is local React memory only and does not imply multi-user coordination, network polling, Flask, SQLite, or production conflict detection.
- The selected V2 feedback-system target is the first displayed ideation result, preserved at `qa/v2-feedback-system/source-feedback-system-option-1.png`. Use differentiated interruption levels rather than forcing every problem into one modal: local save errors remain in the current drawer, expired sessions use one compact blocking dialog, authorization failures use an open page-level state, and network loss uses a thin persistent calendar banner.
- Transient toast feedback carries an explicit semantic tone. Successful operations use the subdued green check, neutral guidance uses the information icon, and failures or blocked actions use the terracotta warning icon; negative copy must never be paired with a success check. Toasts retain `role=status`, polite live announcement, manual dismissal, and timed expiry.
- A failed booking save must keep every field editable and intact. Show the exact copy “保存失败 / 未能保存本次修改，你填写的内容已保留。” immediately above the fixed action area, with real “重试保存” busy/success behavior and a quieter “稍后处理” action that dismisses the error without discarding the form.
- An expired session blocks the underlying application with a compact warm-raised dialog, focuses and traps keyboard navigation on “重新登录”, and uses the precise copy “本标签页内的预约草稿已保留，使用同一账号重新登录后可以恢复”. Before isolation, the booking form and any pending draft are stored in `sessionStorage` under the authenticated user ID. Only a successful session-plus-bootstrap re-authentication for that same user consumes the draft, returns to the calendar, and announces its restoration; another account cannot read it, and active logout clears that account's draft. Do not silently discard content or allow edits behind the expired-session layer.
- Ordinary employees continue to see neither “用户管理” nor “系统状态” in navigation. The explicit `feedbackState=unauthorized` demo may preserve the requested admin-page title inside the authenticated shell, but it must show only “无权限访问此页面”, the specific missing permission, and a working “返回我的预约” action—never account or administrative data.
- “笔录室” is also administrator-only. Employee direct-demo routes to `rooms`, `users`, or `system` fall back to “我的预约” unless the explicit unauthorized-state demonstration is requested. Demo routing defaults to the employee role; administrator access requires `role=admin`.
- The authenticated calendar is an open unit-wide shared schedule. Both ordinary employees and administrators may see the reservation owner, case number, party, purpose, notes, tag, room, and time for every unit booking; calendar cards identify the reservation owner and the detail drawer exposes the complete synthetic record. Visibility does not grant mutation rights: ordinary employees may edit or cancel only bookings whose stable `ownerId` matches the current synthetic account, while administrators may manage unit-wide bookings. Booking identity uses a stable reservation `id`; synthetic optimistic updates also carry a `revision` and never fall back to matching by case number/date. This authenticated-unit rule never broadens the unauthenticated public-display allowlist.
- Calendar network loss keeps the last successful schedule visible and read-only. Show a thin muted-ochre banner with “网络连接已断开”, last update time, and a 44px “重新连接” action; disable date mutation and new-booking slots until recovery, then restore them and announce success. Do not replace blank calendar slots with persistent cards or expose new data while offline.
- Feedback demo routes are `?screen=calendar&feedbackState=save-failed`, `?screen=calendar&feedbackState=session-expired`, `?screen=users&role=employee&feedbackState=unauthorized`, and `?screen=calendar&feedbackState=offline`. All four are React synthetic-memory demonstrations only and do not establish real authentication, permission, network, API, Flask, SQLite, or customer-data behavior.
- New bookings keep a large inherited time summary and expose only duration before the “预约信息” section. Booking edits replace the repeated room/time headline with a quiet party/case identity line; date and room share the first row, start time uses a full-width row, and the duration slider sits below them. Both forms use one-row compact tags, collapse notes by default, and keep the primary submit action persistently visible in a fixed drawer footer.
- Drawer inputs and selects must inherit the product's PingFang/system sans stack, including the selected values for “笔录室”, “开始时间”, and date. Do not let native select styling introduce a mismatched display font.
- Open Personal Center from the bottom account icon; do not add Personal Center itself to the main nav list. Keep the account identity compact in the page header.
- Personal Center is one settings surface without tabs, activity summaries, or a duplicate Data Center link. Service statistics and CSV export belong only to the main-nav Data Center for both roles.
- “数据中心” is a main-nav destination for both roles. Employees receive only the server-enforced self scope. Administrators default to an overall view and may switch to self, another enabled user, or an inactive user retained for history. Never implement this boundary with client-side owner filtering.
- Keep the Data Center as an open editorial analysis canvas with one compact scope/date/filter/export header and page navigation. “概览” alone contains the compact text-only metric band, quiet cancellation guardrail, and dominant interactive trend. Use calendar weeks for inclusive ranges up to 90 days and calendar months for longer ranges. Hover and keyboard focus expose the exact clipped period plus count and duration; duration-mode bar-top labels stay numeric-only while the detail keeps its unit. “时段分布”, “笔录室”, and “标签” are separate focused pages for the overall view; personal views omit the room page. The time page uses a continuous weekday × half-hour dot matrix without row/column guide lines; hovering or focusing a dot with at least two occupied slots reveals its white count inside the dot. The room page uses an unnumbered lollipop composition rather than a ranking. The tag page uses a 100-dot composition where the unassigned category is neutral warm gray and real tags use muted low-saturation accents. Avoid a generic card-wall aesthetic, rankings, scores, red/green performance judgments, room “utilization”, case-count claims, or employee comparisons.
- Never render an all-person workload list. Administrators inspect an employee by using the header view selector; employees receive only their server-enforced self view. CSV settings and detailed filters stay collapsed until requested. Always label the active date range and generation time. Empty/loading/error states remain inside the canvas, and 1024×720 must not create document-level horizontal overflow.
- Personal Center keeps name, department, default duration, default room, default tag, personal tags 3/4, booking-change notification, and booking reminder, with the existing save confirmation. Default tag uses the existing `settings-choice-row`, offers “不指定” plus the current user's four resolved tag labels, and only preselects a genuinely blank future create form; it never overwrites edit or recovered/preserved drafts. Booking reminder exposes a compact 15/30/60-minute selector beside its switch, uses the saved value in the explanatory copy, disables the selector while the switch is off without clearing it, and defaults new accounts to 30 minutes. A quiet “对外提醒模板” section contains one ≤200-character textarea, the five approved variable chips, live synthetic preview, “恢复默认”, and the exact manual-send boundary; reopening Personal Center discards unsaved edits. The booking-detail action area exposes a quiet outlined “复制提醒信息” only for an active, not-started booking the current user may manage, and reports clipboard success or a manual-copy failure without opening an external channel or claiming a sent state. It may also expose the account-scoped browser-only default landing view, with that storage boundary stated in the UI. Remove the obsolete activity-range setting together with the heatmap. Remove phone, email, avatar upload/camera, and password actions from this page. Web reminders are described only as effective while the page is open; do not imply cloud push, SMS, WeChat, or native Windows notifications.
- Settings use dividers only between major sections. Never add separators between preference rows, notification rows, or other small-level items. Keep a slightly larger pause between “个人资料” and “预约偏好” than between ordinary rows.
- The selected data-loading and empty-state target is the first displayed ideation result, preserved at `qa/v2-empty-loading/source-empty-loading-option-1.png`. Keep each state inside its existing page shell: calendar loading retains the date controls, room headers, time axis, and quiet inline skeleton bars; after a date change, bookings remain bound to the date that produced them and the previous date's records must be cleared or isolated before the new date heading is rendered. Empty bookings and empty history use centered Phosphor icons and restrained copy; no available rooms stays inside the open calendar canvas. Do not replace these states with a modal, boxed card, illustration, dashboard, or marketing-style onboarding panel.
- Data-state demo routes are `?screen=calendar&dataState=loading`, `?screen=mine&dataState=empty`, `?screen=history&dataState=empty`, and `?screen=calendar&dataState=no-rooms`. They are synthetic React-memory demonstrations only and do not poll Flask, call an API, read SQLite, or access production/customer data.
- “我的预约” true-empty state says “还没有预约”, keeps the centered hero alignment approved for the normal page, and provides a black 46px “前往预约日历” action. Hide the entire “之后” section and its hairline when the account has no appointments at all; it carries no information in this state. A filter-produced zero result remains a separate “没有符合条件的预约 / 清除筛选” state and may retain the normal page structure; do not conflate it with a genuinely empty account.
- “预约记录” true-empty state keeps the selected month controls and `0 场`, then shows “这个月还没有预约记录” with an outlined 46px “查看其他月份” action. Activating it restores the normal July synthetic records. Search/filter zero results continue to use “没有符合条件的记录 / 清除筛选”.
- When no active recording room exists, the calendar header and date controls stay visible, the canvas says “当前没有可预约的笔录室”, and the black 46px “前往笔录室管理” action opens the existing room-management page. The demo initializes with an empty in-memory room list so creating the first room through the existing inspector restores a real usable room without touching production data.
- All empty-state symbols must remain existing Phosphor icons (`CalendarBlank`, `ClockCounterClockwise`, `DoorOpen`). Loading placeholders are quiet warm-gray interface skeleton bars with no shimmer, gradient, handmade SVG, CSS illustration, emoji, or decorative asset.
- The selected submission-feedback target is the second displayed ideation result, preserved at `qa/v2-submission-feedback/source-submission-feedback-option-2.png`. Keep saving, validation, success, and concurrent-edit feedback inside the existing calendar/drawer flow; do not add a modal stack, separate error page, decorative illustration, or second form system.
- While a booking is saving, keep every field visible and locked in place and replace only the fixed drawer footer with a pale warm status strip using the existing Phosphor `CircleNotch`, exact copy “正在保存预约 / 请稍候”, and no competing action. Normal create/edit submission must expose this busy state before its synthetic in-memory result.
- Booking validation shows one short terracotta summary (`请检查 N 个字段`) above the invalid area plus inline field messages and `aria-invalid`/`aria-describedby`. Focus the first invalid field after submit and remove each error from both the field and count as it is corrected; do not use a generic toast for actionable form errors.
- Successful create/update closes the drawer and places one slim raised-ivory confirmation bar over the top of the calendar. It includes the action, room, exact time, a 44px “查看” action that opens the saved booking, and a close action. Keep this confirmation visually quieter than the primary submit button and do not use a centered modal.
- If another user changes an appointment before an edit is saved, keep the edit drawer and show a fixed lower resolution panel with exact heading “预约内容已发生变化”, a two-column “你的修改 / 最新预约” comparison for room, start time, and duration, plus real actions “使用最新内容”, “返回继续调整”, and “重新检查”. Recheck reads the reservation again by stable ID, exposes a busy state, refreshes the comparison baseline while preserving the local draft, and reports whether another revision appeared; using latest content loads it into the form without discarding identity fields.
- Submission-feedback demo routes are `?screen=calendar&feedbackState=saving`, `?screen=calendar&feedbackState=field-errors`, `?screen=calendar&feedbackState=success`, and `?screen=calendar&feedbackState=modified`. These routes, their competing changes, and all success records are synthetic React memory only; they do not call Flask/API, read or write SQLite, coordinate real users, or access customer data. The final frozen visual contract is defined by the V2 UI freeze snapshot below.

## V2 UI freeze snapshot — 2026-08-09 post-review refreeze with shared-calendar amendment

- This snapshot supersedes the earlier `qa/v2-ui-freeze-2026-08-09/` evidence. The production-integration visual contract is the current React prototype in `src/`, the pure synthetic-state contract in `src/prototype-model.js`, the unaffected numbered screenshots in `qa/v2-ui-refreeze-2026-08-09/`, and the four shared-calendar amendment screenshots in `qa/v2-calendar-shared-details-2026-08-09/`. The old calendar-visibility screenshots `24`–`26` are historical only. Future Flask/API/SQLite integration must preserve this page structure, visual hierarchy, permissions, wording intent, interaction states, public-display privacy allowlist, and accessibility behavior unless a later design-change request explicitly reopens the UI.
- The frozen page set includes login and five login outcomes; the continuous first-run setup flow; user management; system status; the unauthenticated LAN public display; appointment calendar, personal bookings, history, room management, and personal settings; plus loading, empty, saving, validation, success, conflict, offline, expired-session, and unauthorized states.
- Personal settings are limited to name, department, default duration, default recording room, default tag, personal tags, booking-change notification, booking reminder, browser-scoped default landing view, and the personal external-reminder template defined above. Saving updates the current-user profile and server-backed preferences; default duration, room, and tag are consumed only by eligible blank create flows. Phone, email, avatar upload/camera, and password actions are absent. Both web reminders explicitly say they work only while the page is open; external reminder copy remains a manual clipboard action.
- In the authenticated appointment calendar, ordinary employees and administrators can inspect the complete synthetic details of every unit booking, including its reservation owner. Ordinary employees may edit or cancel only their own bookings; administrators may manage unit-wide bookings. The separate history rule remains role-scoped: employees see only their own history, while administrators may inspect unit-wide history and filter by user. Employees still cannot see or direct-route into room, user, or system management. Personal-tag filtering remains dependent on first selecting its owning user, and at least one enabled administrator must always remain, including at the submit-layer invariant.
- First-run completion commits the configured administrator, rooms, and working-hour slots to synthetic SPA memory; the created administrator can sign in immediately. This is an interaction contract only and does not promise browser-reload persistence.
- The visitor display derives from current synthetic rooms/bookings through a pure public-field projection. It may show room, current/next masked visitor, current/next time range, date/time, connection state, and last update only. Full names, staff/owner identities, case numbers, purposes, notes, labels, phones, departments, and all unlisted fields are discarded before rendering. Production integration must enforce the same allowlist server-side.
- Right-side drawers and the expired-session dialog keep minimum 44px targets, visible ≥3:1 focus indicators, background `inert` isolation, first-action/field focus, Tab/Shift+Tab trapping, Esc closure, and trigger focus return. This minimum includes compact actions such as drawer back, reminder acknowledgement, LAN-address copy, and room deletion; their icon artwork may stay visually smaller inside the hit area. Native calendar buttons use a single Tab stop plus coordinate-based arrow-key movement; disabled or occupied cells never change the intended room column or wrap into another row. Hidden notifications and backdrops are not keyboard stops.
- Authenticated non-modal popovers (booking/calendar/history filters, history search/month selection, user search, and audit filters) close on Esc and outside pointer interaction. Esc and an in-popover selection that closes the layer restore focus to the trigger; an outside pointer interaction keeps focus on the newly chosen target instead of stealing it back.
- Low-frequency safeguards remain visible and bounded: drawers make main-region toasts/reminder actions inert; history month controls share one recent-12-month range and single-choice room/tag filters use radio semantics; calendar date navigation is limited to server date ±2 years; profile save validates a non-empty name and exposes a disabled busy state; failed booking-event history has a 44px retry action; cancelled history copy reaches 4.5:1; booking starts are checked against `slotMinutes`; and an employee with no active room receives an explicit administrator-contact message instead of silent navigation.
- Every authenticated main canvas is a focusable scroll container with a visible focus indicator and scrollbar. Keyboard users can reach it and scroll long pages with Page Up, Page Down, or Space.
- Informational text tokens meet at least 4.5:1 on the main warm canvas; meaningful status/tag/health dots have a contrasting outline. At 1024×720, an open user drawer preserves the selected user's role/status preview; 720px-high booking controls remain at least 44px; a three-room calendar fits the 852px schedule viewport without an 8px horizontal scrollbar or document overflow. Calendars with more rooms may scroll inside the schedule viewport while the document itself remains fixed to the viewport.
- The frozen desktop support set is 1024×720, 1280×720, 1440×900, and 1920×1080. The existing 960px, 720px, and 620px rules are defensive fallbacks that prevent severe clipping when a desktop window is narrowed; they are not acceptance targets and do not establish mobile or tablet support. Mobile product redesign remains outside this contract.
- `npm run test:prototype` is the front-end contract suite for time-slot generation, public-display masking/projection, authenticated unit-wide detail visibility, role/ownership-based management, and stable identity. `npm run build` and `npm run test:sites` remain required packaging checks.
- All content and state remain synthetic React memory. This snapshot authorizes no deployment and no connection to Flask, API, SQLite, production/customer data, secrets, backup services, notification services, or an AI experiment area. Final audit evidence and verification details are recorded in `design-qa.md`; do not claim backend, authentication, authorization, real concurrency, real networking, TV hardware, or customer-data acceptance from this prototype.
