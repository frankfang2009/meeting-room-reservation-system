# 版本更新记录

## V2.1.0 定制开发（未发布）

### 界面布局收尾

- 系统状态页的“工作时间”改为与“最近备份”一致的整行可点击入口，抽屉保存区与时间输入区拉开间距。
- 个人中心把账户摘要收进页头，活动概览与四项数据改为上下两条横向通栏，并保留窄桌面窗口的有序折行。
- 修复预约侧栏中较长的个人标签名称越过按钮边界、与相邻标签或备注区域重叠的问题。

### C0 Schema v1 → v2 迁移框架

- 新鲜安装直接创建 schema v2，并为笔录室公示开关、默认标签、提醒提前量与个人对外提醒模板预留受约束字段。
- 已有产品代际 2/schema v1 数据库在服务就绪前事务化迁移；列已存在时可安全重试，任一步失败则整体回滚并 fail-closed。
- 恢复器继续接受 schema v1 受保护备份，原子替换后复用同一迁移；迁移失败会恢复原现场。

### C1 管理员可修改工作时间

- 系统状态页增加安静的工作时间编辑抽屉，只允许管理员保存按时段对齐的开始和结束时间。
- 新工作时间仅影响后续可用时段和集成可用性；已有预约不改写，落在新工作时间外时仍可在日历与历史中查看。
- 工作时间修改写入 `settings.updated` 安全审计，失败时整个事务回滚。

### C2 默认预约标签

- 个人中心可选择不指定或标签槽位 1–4 作为默认标签，标签改名后仍跟随同一槽位。
- 默认标签只预选之后打开的空白新建预约；会话恢复、冲突和跨页保留的草稿不会被覆盖。

### C3 公开大屏按笔录室隐藏

- 管理员可在笔录室编辑区控制该房间是否出现在公开大屏，新建房间默认显示。
- 被隐藏房间及其预约在公开大屏 SQL 查询阶段即被排除；全部隐藏时大屏显示明确空态。
- 员工共享日历与只读集成 API 保持不变，`showOnDisplay` 不进入员工或公开响应。

### C4 提醒提前量

- 个人中心可把网页内预约提醒设置为开始前 15、30 或 60 分钟，默认仍为 30 分钟。
- 提醒查询与确认始终读取同一份当前偏好；缩短提前量后，已不在当前窗口内的旧提醒不能再确认。
- 关闭预约提醒时保留已选提前量，重新开启后继续使用。

### C5 一键复制对外提醒信息

- 未开始的本人预约（管理员可操作全单位预约）在详情中提供“复制提醒信息”，按预约姓名、日期、时间和笔录室拼装后仅写入剪贴板，由用户自行发送。
- 个人中心支持不超过 200 字的本人模板、5 个安全变量、光标插入、实时示例预览和恢复默认；案号、用途、备注不进入变量集合。
- 不自动发送、不跳转微信、不记录已发送状态，不提供每房间、多模板或预约级模板。
- 系统默认模板已按计划示例落地，默认文案待用户定稿。

## V1.0.2-r1（修复更新通道）

### 为什么修订

- 原 V1.0.2 使用 `Start-Process -Verb RunAs` 携带参数重新执行 BAT；客户文件名带
  `(1)` 时会落入 `cmd.exe` 的特殊引号解析路径，管理员子进程在主升级逻辑开始前
  退出 1。
- 原 GitHub Windows 测试直接在管理员 runner 中调用 BAT，绕过了真实入口、UAC 和
  ShellExecute，因此没有覆盖这一故障路径。

### 新更新方式

- 改为完整 ZIP：零参数 BAT 只启动包内冻结 `python.exe`，Python 再直接通过
  `ShellExecuteExW` 提权同一个 `python.exe`；不再 RunAs BAT，不经过带参 `cmd /c`，
  也不使用 broker 参数握手。
- 包内同时固定携带冻结 V1.0.1 受管基线、V1.0.2 完整目标和冻结 runtime，三者均有
  逐文件清单与树哈希。
- 当前真实 `data` 始终是唯一权威源；更新器只复制 data，在副本上执行迁移检查和回环
  健康检查，不写回真实 data。
- 每次修复先把受管程序实际恢复并验证为冻结 V1.0.1，再覆盖 V1.0.2；目标失败时严格
  停在 V1.0.1。
- 停机前先持久化任务配置，同时持有新旧升级锁；旧 `_升级回滚` 仅作证据，不用于覆盖
  当前 data。
- `版本.txt` 仍是最后提交点，并补齐“版本已写入、状态尚未来得及更新”和
  `complete` 后快照已清理两类断电重入窗口。
- 只有 SHA-256 与原故障正式 BAT 完全一致的 `升级到V1.0.2*.bat` 才会被停用归档，
  不再用宽泛文件名匹配移动用户脚本。
- 特殊路径测试覆盖中文、空格、括号和 `&`；新增负载篡改、停机失败、空间不足、
  半覆盖、提交窗口、回退后新增数据以及损坏 runtime 的故障注入。

### 操作变化

- 客户解压完整修复 ZIP，双击“修复并更新到V1.0.2.bat”并同意 UAC。
- 修复成功后再双击原系统的“① 启动系统.bat”。管理员更新器不替用户猜测启动身份。
- 原 `升级到V1.0.2.bat` 和原发布清单保持冻结，只作历史证据，不再外发。

## V1.0.2（正式版）

### 用户体验

- 当前半小时的空闲时段显示“已开始”，更早时段显示“已过期”，均不能再预约。
- 服务端在提交前和获得写锁后两次校验开始时间，避免等待期间产生过期预约。
- 今天的日历标出“现在”并自动定位到当前工作时间附近。
- 手机端自动定位会优先保证当前时间行留在视口内。
- 日期增加中文星期显示。
- 预约表单自动排除无效时间组合，并在当天无剩余时段时给出明确提示。
- 成功和普通信息提示自动消失；错误及警告继续保持可见。
- 静态资源使用内容指纹，升级后浏览器会及时加载新样式与新交互。
- 预约结束时刻一到即统一显示为“已完成”。
- 局域网地址变化后仅管理员看到持续提醒，可对照旧、新地址并一键复制；确认已通知同事后提醒才消失。
- 同事改用新地址后即使需要重新登录，账号、会议室和预约数据也保持不变。

### 发布与维护

- `源代码工作区` 成为程序文件的唯一来源。
- 新增 `准备发布.py`，自动组装完整累计负载、候选 BAT、Windows 候选目录和发布清单。
- 新增稳定 `发布骨架`，历史 V1.0.1 负载和候选物不再承担未来源码职责。
- 发布组装会锁定冻结 runtime，并对静态资源中的敏感文件、未知类型和危险输出路径直接失败。
- 候选 BAT 与发布清单成对交付；任一写入失败会恢复旧文件，不留下新旧混搭。
- 普通用户双击入口改为独立、可校验的 PowerShell 代理，不再把提权和服务交接逻辑
  塞进一条超长命令；UAC 或安全策略异常会显示错误并保留入口日志，不再静默关窗。
- Windows CI 改为读取当前版本，核对源码、负载、候选 BAT，并同时验证
  V1.0.0→V1.0.2 与 V1.0.1→V1.0.2。
- 数据库结构仍为版本 `1`，没有数据迁移。
- 升级成功摘要增加当前版本、数据保留结果、最终运行状态和局域网地址结果。
- 升级前暂时禁用属于本安装的开机任务；版本提交前只做回环维护检查，不向局域网开放可能被回滚的新版服务。
- 每套安装使用独立安装身份；健康检查不再把占用 8080 的其他程序或另一套安装误判为本系统。
- 同一电脑发现多套有效安装时不再猜测目录；仅支持本机固定磁盘，不支持共享盘、U 盘、移动硬盘或网盘同步目录。
- 成功事务清理完整数据回滚快照，失败或未完成事务继续保留恢复及诊断证据。
- 使用说明明确短暂停机、在途请求刷新、地址变化后重新登录，以及 UAC、SmartScreen、杀毒软件/EDR、AppLocker、VPN、DHCP 和防火墙的实机边界。

### 发布状态

- 2026-07-26 正式收口；用户已确认现有客户环境从旧版本升级成功。
- GitHub Windows PowerShell 5.1 自动化验证通过，覆盖 V1.0.0/V1.0.1 累计升级、
  重复运行、数据保留、故障回滚和候选发布物一致性。
- 实机成功结论仅对应当前客户环境；部署到其他 Windows 10/11、安全策略或网络环境时，
  仍须核对 UAC、SmartScreen、杀毒软件/EDR、AppLocker、防火墙和局域网访问。
- 正式 BAT 与发布清单沿用已验收候选物的原始字节，不重新生成或覆盖历史文件。

## V1.0.1（冻结候选基线）

- 建立单文件累计升级、程序与数据快照、失败回滚、健康检查和最终版本提交机制。
- 自动化验证已通过，候选 BAT 和部署目录保持冻结，供 V1.0.2 跨版本测试使用。

## V1.0.0

- 第一个正式归档和客户交付版本。
