会议室预约系统 V1.0.1 候选版说明
================================

状态
----

升级机制和 V1.0.1 累计负载已在 macOS 开发机实现并完成自动化验证。
当前仍是“待 Windows 验收”候选版，不得作为正式客户版本归档或发送。


候选交付物
----------

1. 单文件升级包：
   升级包工具\输出-待Windows验收\升级到V1.0.1.bat

2. 完整部署候选目录：
   Windows部署目录-V1.0.1-待Windows验收

3. 出包工具、累计负载和维护说明：
   升级包工具


成品校验信息
------------

升级 BAT SHA-256：
b9f36530f16327e007d76b8bad4ac39f6d6d86588635972c14dcb817e26de3c7

内嵌 ZIP SHA-256：
330ba7a1d65413147f65bccc49d939da8ba9f008bc3492411b7ab8072d2865ff

升级 BAT 大小：104455 字节
内嵌 ZIP 大小：36913 字节
累计负载文件：25 个

SHA-256 用于发现传输损坏或误编辑，不是数字签名。


本机已通过
----------

- 业务、权限、并发、备份和数据库迁移测试：37 项。
- 出包、路径安全、可重复构建和关键恢复契约测试：22 项。
- 总计：59 项自动化测试全部通过。
- 候选部署脚本完成 Python 语法编译检查。
- 候选数据库迁移连续运行两次均成功，结构版本为 1，管理员基线只保留一份。
- 最终 BAT 已反向抽取、解码和解包；25 个文件与累计负载逐字节一致。
- 同一输入重复出包字节完全一致；成品为 UTF-8 无 BOM、全 CRLF，两个整行标记各一个。
- 累计负载 25 个文件与完整部署候选目录中的对应文件逐字节一致。
- V1.0.1 内嵌 runtime 与 V1.0.0 参考部署目录逐字节一致，本期未升级依赖。
- V1.0.0 正式归档中的三个主要文件 SHA-256 与原 SHA256SUMS.txt 一致，旧归档未改写。
- 升级事务脚本经过独立冷审，未发现剩余阻断项。


正式发布前必须完成
------------------

必须在真实 Windows 10/11、Windows PowerShell 5.1 环境完成实施计划中的 15 项人工验收，
重点覆盖：UAC、成功升级、重复运行、计划任务三种状态、数据库迁移失败、服务启动失败、
并发双击，以及 preparing、快照、程序覆盖、迁移、健康检查和回滚过程中的强杀/断电恢复。

Windows 验收通过后，才可以：

1. 去掉“待Windows验收”标记；
2. 生成正式完整部署 ZIP；
3. 新建 V1.0.1 版本归档；
4. 向客户发送升级 BAT、SHA-256 和一句话操作说明。

V1.0.0 归档继续保持只读，不得覆盖。
