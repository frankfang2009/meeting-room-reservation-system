# 贡献指南

感谢你愿意改进会议室预约系统。项目优先接受范围明确、可验证、不会削弱数据与权限边界的修改。

## 开始之前

1. 搜索现有 Issue，确认问题没有重复；安全漏洞不要创建公开 Issue，请按 [安全策略](.github/SECURITY.md) 私下报告。
2. 较大的功能或产品行为变更先创建提案 Issue，说明使用场景、边界、数据影响和替代方案。
3. 从 `main` 创建短生命周期分支，不要提交数据库、日志、备份、密钥、真实姓名或客户资料。

## 本地验证

完整开发环境与门禁：

```bash
v2/scripts/bootstrap-dev.sh
v2/scripts/check.sh
```

门禁要求精确的 Python 3.13.14、Node.js 22.17.1 和锁定依赖。Pull Request 还会在 Linux 与 Windows 上运行独立检查。

## 修改要求

- 业务代码必须补对应的自动化测试；修复缺陷时优先先写能复现问题的回归测试。
- API、权限、数据库字段或错误语义变化需同步 `v2/docs/API-CONTRACT.md` 和 `v2/docs/PRODUCT-CONTRACT.md`。
- 可见界面或交互变化需同步 `v2/frontend/DESIGN-CONTRACT.md`；发布流程变化需同步 `v2/docs/RELEASE-CHECKLIST.md`。
- 预约、占用时段和事件必须保持同事务；数据库身份、完整性和未知版本必须 fail-closed。
- 公开大屏的字段白名单和脱敏必须在服务端完成，不得在浏览器把私有响应“删字段后公开”。
- V2 不读取、迁移、修改或删除 V1 数据。`02_开发工作区/` 只接受明确的 V1 兼容或安全维护。
- 不加入演示密码、生产种子数据、自动外发消息或未经批准的远程服务。

## Pull Request

请保持一个 PR 只解决一个主题，并在模板中填写：问题、方案、测试、契约/文档变化、安全与数据影响、截图或“不适用”。维护者可能要求拆分过大的改动。

提交贡献即表示你有权提交相关内容，并同意贡献按仓库的 Apache-2.0 许可证发布。不要提交来源或授权不清晰的图片、字体、代码或文案。
